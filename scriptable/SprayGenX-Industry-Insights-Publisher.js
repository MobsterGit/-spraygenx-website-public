// SprayGenX Industry Insights Publisher
// Version: 2026.08.01
// Paste a ChatGPT-generated JSON briefing from the clipboard, preview it, and publish it.

const CFG={owner:"MobsterGit",repo:"-spraygenx-website-public",branch:"main",tokenKey:"sgx.github.industry.publisher",folder:"data/regional-updates",site:"https://spraygenx.com/regional-updates.html"};
const fm=FileManager.iCloud();
const localDir=fm.joinPath(fm.documentsDirectory(),"SprayGenX/IndustryInsights");
const draftPath=fm.joinPath(localDir,"draft.json");
if(!fm.fileExists(localDir))fm.createDirectory(localDir,true);

async function msg(title,message){const a=new Alert();a.title=title;a.message=message;a.addAction("OK");await a.presentAlert();}
async function yes(title,message,action="Continue",danger=false){const a=new Alert();a.title=title;a.message=message;danger?a.addDestructiveAction(action):a.addAction(action);a.addCancelAction("Cancel");return await a.presentAlert()===0;}
function niceDate(iso){const [y,m,d]=iso.split("-").map(Number);const f=new DateFormatter();f.locale="en_US";f.dateFormat="MMMM d, yyyy";return f.string(new Date(y,m-1,d));}
function esc(s){return String(s??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");}
function validate(a){for(const k of ["title","date","summary","sections","takeaway","sources"])if(!(k in a))throw new Error(`Missing ${k}`);if(!Array.isArray(a.sections)||!a.sections.length)throw new Error("At least one section is required.");if(!Array.isArray(a.sources))throw new Error("Sources must be an array.");for(const s of a.sections)if(!s.heading||!s.content)throw new Error("Each section needs heading and content.");return a;}
async function readDraft(){if(!fm.fileExists(draftPath))return null;if(fm.isFileStoredIniCloud(draftPath)&&!fm.isFileDownloaded(draftPath))await fm.downloadFileFromiCloud(draftPath);return JSON.parse(fm.readString(draftPath));}
function saveDraft(a){fm.writeString(draftPath,JSON.stringify(a,null,2));}

async function token(){if(Keychain.contains(CFG.tokenKey))return Keychain.get(CFG.tokenKey);const a=new Alert();a.title="GitHub Token";a.message="Paste your existing fine-grained token. It stays in iPhone Keychain.";a.addSecureTextField("Token");a.addAction("Save");a.addCancelAction("Cancel");if(await a.presentAlert()!==0)return null;const t=a.textFieldValue(0).trim();if(!t)return null;Keychain.set(CFG.tokenKey,t);return t;}
async function credential(){const a=new Alert();a.title="GitHub Credential";a.message=Keychain.contains(CFG.tokenKey)?"Token saved in iPhone Keychain.":"No token saved.";a.addAction("Replace Token");a.addDestructiveAction("Remove Token");a.addCancelAction("Done");const c=await a.presentSheet();if(c===0){const e=new Alert();e.title="Replace Token";e.addSecureTextField("Token");e.addAction("Save");e.addCancelAction("Cancel");if(await e.presentAlert()===0){const t=e.textFieldValue(0).trim();if(t){Keychain.set(CFG.tokenKey,t);await msg("Saved","GitHub token updated.");}}}else if(c===1&&Keychain.contains(CFG.tokenKey)&&await yes("Remove Token?","Remove the saved token from this iPhone?","Remove",true)){Keychain.remove(CFG.tokenKey);await msg("Removed","Token removed from Keychain.");}}

async function pasteDraft(){const raw=Pasteboard.pasteString()?.trim();if(!raw){await msg("Clipboard Empty","Copy the structured JSON briefing from ChatGPT, then try again.");return;}let article;try{article=validate(JSON.parse(raw));}catch(e){await msg("Invalid Draft",String(e.message||e));return;}saveDraft(article);await msg("Draft Saved",`${article.title}\n${niceDate(article.date)}\n\n${article.sections.length} sections • ${article.sources.length} sources`);await preview(article);}

async function preview(existing=null){const a=existing||await readDraft();if(!a){await msg("No Draft","Paste a draft first.");return;}validate(a);const sections=a.sections.map(s=>`<section><h2>${esc(s.heading)}</h2><p>${esc(s.content)}</p></section>`).join("");const sources=a.sources.length?a.sources.map(s=>`<li><a href="${esc(s.url)}">${esc(s.title)}</a></li>`).join(""):"<li>No sources listed.</li>";const html=`<!doctype html><meta name="viewport" content="width=device-width,initial-scale=1"><style>body{font-family:-apple-system;margin:0;background:#eef2f6;color:#142337}main{max-width:760px;margin:auto;padding:18px 14px 60px}.hero{background:#0d2947;color:#fff;padding:24px;border-radius:18px}.k{color:#74b9ff;font-size:12px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}h1{font-size:29px;line-height:1.1;margin:9px 0}.date{opacity:.75}.summary{font-size:17px;line-height:1.5}section{background:#fff;margin-top:14px;padding:20px;border-radius:15px;box-shadow:0 4px 16px rgba(15,45,75,.08)}h2{color:#0d6099;font-size:20px;margin:0 0 10px}p{line-height:1.55;margin:0}.take{border-left:5px solid #1380c4}li{margin:8px 0}a{color:#0878bd}</style><main><header class="hero"><div class="k">Northeast Ohio Industry Insights</div><h1>${esc(a.title)}</h1><div class="date">${esc(niceDate(a.date))}</div><p class="summary">${esc(a.summary)}</p></header>${sections}<section class="take"><h2>Practical Takeaway</h2><p>${esc(a.takeaway)}</p></section><section><h2>Sources</h2><ol>${sources}</ol></section></main>`;const w=new WebView();await w.loadHTML(html);await w.present(true);}

async function gh(path,t,method="GET",body=null){const r=new Request(`https://api.github.com/repos/${CFG.owner}/${CFG.repo}${path}`);r.method=method;r.headers={Accept:"application/vnd.github+json",Authorization:`Bearer ${t}`,"X-GitHub-Api-Version":"2022-11-28","User-Agent":"SprayGenX-Scriptable-Publisher"};if(body){r.headers["Content-Type"]="application/json";r.body=JSON.stringify(body);}let data=null;try{data=await r.loadJSON();}catch{}return{status:r.response?.statusCode||0,data};}
async function getFile(path,t){const x=await gh(`/contents/${path}?ref=${encodeURIComponent(CFG.branch)}`,t);if(x.status===404)return null;if(x.status<200||x.status>=300)throw new Error(x.data?.message||`Unable to read ${path}`);return x.data;}
async function putFile(path,text,t,message,sha=null){const body={message,content:Data.fromString(text).toBase64String(),branch:CFG.branch};if(sha)body.sha=sha;const x=await gh(`/contents/${path}`,t,"PUT",body);if(x.status<200||x.status>=300)throw new Error(x.data?.message||`Unable to write ${path}`);}

async function publish(){const a=await readDraft();if(!a){await msg("No Draft","Paste a draft first.");return;}validate(a);const t=await token();if(!t)return;if(!await yes("Publish Industry Insights?",`${a.title}\n${niceDate(a.date)}\n\nThis publishes directly to the live website.","Publish"))return;const file=`${a.date}.json`;const articlePath=`${CFG.folder}/${file}`;const indexPath=`${CFG.folder}/index.json`;const oldArticle=await getFile(articlePath,t);await putFile(articlePath,JSON.stringify(a,null,2)+"\n",t,`Publish Northeast Ohio Industry Insights for ${a.date}`,oldArticle?.sha||null);const oldIndex=await getFile(indexPath,t);let index={updated_at:null,updates:[]};if(oldIndex?.content){const decoded=Data.fromBase64String(oldIndex.content.replace(/\n/g,"")).toRawString();index=JSON.parse(decoded);}index.updated_at=new Date().toISOString();index.updates=[file,...(index.updates||[]).filter(x=>x!==file)].slice(0,52);await putFile(indexPath,JSON.stringify(index,null,2)+"\n",t,`Update Industry Insights archive for ${a.date}`,oldIndex?.sha||null);const done=new Alert();done.title="Published";done.message="The article was committed to GitHub. GitHub Pages may take a minute or two to refresh.";done.addAction("Open Website");done.addAction("Done");if(await done.presentAlert()===0)Safari.open(CFG.site);}

async function clearDraft(){if(!fm.fileExists(draftPath)){await msg("No Draft","No local draft exists.");return;}if(await yes("Delete Local Draft?","This does not remove anything already published.","Delete",true)){fm.remove(draftPath);await msg("Deleted","Local draft removed.");}}

async function main(){const d=await readDraft();const a=new Alert();a.title="Industry Insights Publisher";a.message=d?`Draft: ${niceDate(d.date)}\n${d.title}`:"No local draft saved.";a.addAction("Paste Draft from Clipboard");a.addAction("Preview Draft");a.addAction("Publish Draft");a.addAction("GitHub Credential");a.addDestructiveAction("Clear Local Draft");a.addCancelAction("Close");const c=await a.presentSheet();try{if(c===0)await pasteDraft();else if(c===1)await preview();else if(c===2)await publish();else if(c===3)await credential();else if(c===4)await clearDraft();}catch(e){console.error(e);await msg("Publisher Error",String(e.message||e));}}

await main();
Script.complete();
